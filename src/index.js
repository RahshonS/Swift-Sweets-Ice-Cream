import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from "./App"

//reactDom.render(want we want to create, where we want it to render)
//createElement(tag, properties we want to add to tag , value);
ReactDOM.render(<App />, document.getElementById("root")
   


);


